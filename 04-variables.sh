#!/bin/bash

# What is a variable 
# A variable is somthing that holds data dynamically
a=100
b=abc
# The advantage and disadvantate in shell is that there are no Data Types
# In bash, everything iwill be considered as string by default.
# How can we print a variable ?$var

echo pinting the value of $a
echo pinting the value of $(b)
